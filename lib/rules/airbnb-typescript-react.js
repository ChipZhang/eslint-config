/**
 * used to pick React related rules from `eslint-config-airbnb-typescript`
 */

const airbnbTypeScriptReactRules = require('eslint-config-airbnb-typescript').rules

module.exports = {
	rules: airbnbTypeScriptReactRules,
}
